spring.application.name=demo

spring.datasource.url=jdbc:mysql://localhost:3306/lentaldb?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=230724
# 여기에 본인의 MySQL 비밀번호 입력

# 2. 실행 시 콘솔창에 SQL 쿼리문을 예쁘게 보여주는 설정 (공부할 때 매우 유용합니다)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# 3. 중요: 자바 엔티티와 DB 테이블을 자동으로 맞춰주는 설정
# update: 바뀐 부분만 DB에 반영합니다.
spring.jpa.hibernate.ddl-auto=update

# 4. MySQL 전용 문법 사용
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect